<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.UserMapper">
    <insert id="registerUser" parameterType="User">
        insert into user(userName, password, email, phone) values (#{userName}, #{password}, #{email}, #{phone});
    </insert>
    <select id="selectUserList" resultType="User">
        select userId, userName, email, phone, userImg from user;
    </select>
    <select id="selectUserByName" resultType="User" parameterType="String">
        select * from user where userName = #{userName};
    </select>
</mapper>