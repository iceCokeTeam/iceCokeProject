<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.CategoryMapper">
    <select id="selectCategoryListByType" resultType="Category">
        select * from category
        <where>
            <if test="name != null and name != ''">
                and name like concat('%', #{name}, '%')
            </if>
            <if test="parentId != null and parentId != ''">
                and parentId = #{parentId}
            </if>
        </where>
    </select>
    <insert id="insertCategory" parameterType="Category">
        insert into category(name, parentId, createTime) values (#{name}, #{parentId}, #{createTime})
    </insert>
    <delete id="deleteCategory" parameterType="int">
        delete from category where id = #{id};
    </delete>
    <update id="updateCategory" parameterType="Category">
        update category set name = #{name} and parentId = #{parentId} and updateTime = #{updateTime};
    </update>
</mapper>